# Pi-hole environment configuration (template for envsubst)

# Pi-hole's web interface HTTP port
PIHOLE_WEB_PORT=${PIHOLE_WEB_PORT}

# Pi-hole's HTTPS port
PIHOLE_HTTPS_PORT=${PIHOLE_HTTPS_PORT}

# FTLconf Web-server API password
WEBPASSWORD=${WEBPASSWORD}

# Absolute host path for Docker bind volume (ZFS mountpoint)
PIHOLE_DATA_VOLUME=${PIHOLE_DATA_VOLUME}
